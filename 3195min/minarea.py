

def minimumArea(grid):
    rows = len(grid)
    col = len(grid[0])
    for i in range(rows):
        for j in range(col):
            print(grid[i][j])
            

        








grid = [[0,1,0],[1,0,1]]
minimumArea(grid)